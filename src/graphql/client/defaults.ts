//For apollo-link-state | delete if not needed

const defaults = {
    currentPoll: {
        __typename: "CurrentPoll",
        title: '',
        refId: '',
        creator: '',
        options: []
    }
}

export default defaults; 